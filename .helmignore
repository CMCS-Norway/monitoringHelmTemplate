# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/

# Kustomize-related files (no longer needed)
kustomization.yml

# Original manifest directories (keep for reference but don't package)
manifests/

# CI/CD files
.github/
.gitlab-ci.yml
.travis.yml
.circleci/

# Documentation
OWNERS
CONTRIBUTING.md
